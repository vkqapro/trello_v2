services:
  E2E:
    image: python:3.8-alpine
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/automation
    command: /bin/sh -c "ls -ls && pip install chromedriver-autoinstaller selenium pyvirtualdisplay && pytest --version && pytest -m login"
    working_dir: /automation