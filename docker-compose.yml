services:
  E2E:
    image: python:3.12.0-alpine3.18
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/automation
    command: /bin/sh -c "ls -ls && playwright install && pytest -m tc100"
    working_dir: /automation